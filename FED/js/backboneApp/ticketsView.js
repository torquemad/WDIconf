var TicketsView = Backbone.View.extend({
  tagName: 'tr',

  className: 'confirmationModalStyle',
  
  initialize: function(){
    
  },

  events:{
  },

  render: function(){
    // var ticketsTemplate = _.template( $('#tickets-template').html());
    // var model = chosenTalks.toJSON();
    // var title = model['title'];
    // var speaker = model['speaker']['name'];
    // var startTime = model['start_time'];
    // var endTime = this.endTime(startTime);
    // this.$el.html(talksTemplate({title: title, speaker: speaker, startTime: startTime, endTime: endTime}));
    // return this;
  }
});