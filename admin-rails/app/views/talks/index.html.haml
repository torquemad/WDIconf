%h2 All Talks

- @talks.each do | talk|

  %p= talk.title
  %p= talk.blurb
  %p
    =link_to 'Edit', "/talks/#{talk.id}/edit"
    / by
    / = talk.speaker.name
    / %a{:href => "#{talk.id}/profile"} Show
    / %a{:href => "#{talk.id}/edit"} Edit
